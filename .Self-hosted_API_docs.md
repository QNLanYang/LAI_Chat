# QNLY API 简要使用说明与模型能力速览

> [!IMPORTANT]
>
> 本文档旨在帮助您快速接入由 **全能岚漾** 自托管的 **公益性** API 端口，该端口无需 API Key 即可使用。使用本服务即代表您已阅读、理解并同意以下所有条款。
>
> **本服务按“现状”提供，不构成任何明示或暗示的担保。因使用本服务产生的任何数据丢失、决策失误等风险与后果，均由您自行承担。**

> [!WARNING]
> 
> **免责声明**
>
> - **内容不构成建议**：AI 生成的内容可能存在事实错误或逻辑谬误，**不构成**任何形式的专业建议。请您自行甄别。
> - **代码须经审查**：AI 生成的代码可能包含安全漏洞或逻辑缺陷，在应用于生产环境前，**必须**进行严格审查与测试。
> - **用途务必合法**：您在使用时必须遵守所在地法律法规。**严禁**用于或诱导模型生成任何涉及 *违法犯罪、血腥暴力、低俗淫秽* 等非法有害内容。

> [!NOTE]
> 
> **隐私与数据说明**
>
> - 本服务部署于个人服务器，由 **LM Studio** 提供模型端口，**Hugging Face** 社区提供开源模型权重，**Nginx + Cloudflare** 作为公网反向代理。
> - **请求内容**：我已在 LM Studio 中关闭请求体记录，故 **API 请求的完整内容（对话文本）不会被记录**。
> - **保留的日志**：
>     - LM Studio 仅记录“某模型加载时间”、“收到请求（内容已隐藏）”、“推理完成（Token 数/速度）”等运行状态。
>     - Nginx 代理会保留基础的访问日志，包含 **来源 IP、请求方法、响应体大小、响应时间** 等。
> - **日志用途与清理**：所有日志仅存储于本地，用于服务监控与排错，并会 **定期清理**。
> - **郑重承诺**：我本人 **绝不会** 主动查阅、泄露或向第三方提供您的任何日志记录，并做好应有的网络安全防范工作。请您综合权衡隐私风险后使用。

---

## API 地址与端点

- **API 基础地址**：`https://api.qnly.top/v1`
- **后端推理软件**：[LM Studio](https://lmstudio.ai/) (下文简称 LMS)

通过 `GET /v1/models` 可获取当前可用模型列表。

> [!NOTE]
>
> LMS 会列出所有可用模型，并采用 **即时加载（JIT-Load）** 机制：
>
> - 当收到某模型的请求时，若该模型未处于显存/内存中，则立即开始加载，加载完成后响应请求。
> - 请求完成后，模型不会立即退出，而是会维持一段 **TTL（空闲存活时间）**，通常为 30 或 60 分钟。在 TTL 内若无新请求，模型将自动卸载以释放资源。
> - 若在 JIT 加载新模型时已有其他模型驻留，则会 **自动卸载先前驻留的模型**。
>
> 因此，**同一时间只能有一个模型处于加载状态**。若您连续使用同一模型，通常只需在首次请求（或间隔超 TTL 后）等待加载；若频繁切换模型，或多人同时请求不同模型，则会引发频繁的加载/卸载与资源竞争，显著增加排队等待时间。

本服务开放了以下主流兼容端点，可使用标准客户端直接连接：
- OpenAI 兼容：`/chat/completions`、`/responses`
- Anthropic 兼容：`/messages`

> [!NOTE]
> 
> 出于安全考量，LM Studio 的原生 REST API 未对公网开放。上述端点已完全覆盖常用功能。
> 
> 详情可参考 [LM Studio 官方文档](https://lmstudio.ai/docs/developer)。

---

## 可用模型与能力参数

### 低配机器模型

| 模型 / Models | 核心能力 / Abilities | 上下文长度 / Tokens | 推理速度 / Tps | 最大并发数 / Concurrency |
|:--|:-:|--:|--:|--:|
| **Qwen3.5**-0.8B (低配) | `👀 视觉` `🔧 工具调用` | 6,000 | 20 / 40 | 2 |

### 高配机器模型

| 模型 / Models | 核心能力 / Abilities | 上下文长度 / Tokens | 推理速度 / Tps | 最大并发数 / Concurrency |
|:--|:-:|--:|--:|--:|
| **Qwen3.5**-0.8B (高配) | `👀 视觉` `🔧 工具调用` | 16,384 | 330 / 490 | 16 |
| **Qwen3.5**-9B | `🧠 推理` `👀 视觉` `🔧 工具调用` | 131,072 | 90 / 155 | 8 |
| **Gemma4**-E4B | `🧠 推理` `👀 视觉` `🔧 工具调用` | 131,072 | 105 / 190 | 8 |
| **Qwen3.6**-35B-MoE | `🧠 推理` `👀 视觉` `🔧 工具调用` | 65,536 | 70 / 85 | 4 |
| **Gemma4**-26B-MoE | `🧠 推理` `👀 视觉` `🔧 工具调用` | 65,536 | 55 / 75 | 4 |

> [!TIP]
> 
> **服务可用性**：除低配版 0.8B 外，其他模型**不保证** 7×24 小时在线，感谢理解。
>
> **0.8B 模型使用逻辑**：因两个 0.8B 模型的 ID 相同，系统无法指定。高配机器在线时将自动启用高配版，离线则回退至低配版。
>
> **速度指标说明**：“推理速度”列中，` / ` 前后的数值分别代表 **单一请求** 和 **同时两个请求** 时的总输出速度，单位均为 `Tokens/s` (Tps)。
>
> **并发数说明**：“并发”指该模型可同时处理的**最大请求数量**。超过此限制的请求将进入排队状态，可能触发超时。

### 模型 ID 对照表

API 返回的模型 ID 以 `id` 字段为准，下表是常见对照：

| 模型友好名称 | 模型 `id` |
|---|---|
| Qwen3.5-0.8B | `qwen3.5-0.8b` |
| Qwen3.5-9B | `qwen/qwen3.5-9b` |
| Qwen3.6-35B-MoE | `qwen/qwen3.6-35b-a3b` |
| Gemma4-E4B | `google/gemma-4-e4b` |
| Gemma4-26B-MoE | `google/gemma-4-26b-a4b` |

---

## 常见问题

**Q: 是否需要付费或获取 Key？**
- A: 不需要。本服务完全公益免费，旨在技术分享与测试。

**Q: 我的对话内容会被看到吗？**
- A: 不会。我已主动关闭了 LM Studio 的请求内容记录，代理日志中也仅含连接元数据（IP、时间等），无法看到您的对话文本。所有日志定期清理，且我承诺不会主动查阅。

**Q: 使用时遇到超时或错误怎么办？**
- A: 通常是模型正在加载或请求排队。请稍等片刻后重试。若持续报错，可能是并发已满或服务维护中，可稍后再试。常见错误码：`429`（请求过多/并发限制）、`503`（服务繁忙/模型加载中）。